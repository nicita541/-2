# Task Manager Web

```bash
cd web
npm install
npm run dev
```

Default API URL is `http://localhost:5088`. Override with `VITE_API_BASE_URL`.

Demo user after backend dev seed:

- email: `demo@taskmanager.local`
- password: `Demo12345!`
